@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">¡Algo salió mal!</h1>
<p>Ocurrió un error inesperado al procesar tu solicitud. Por favor, inténtalo de nuevo más tarde.</p>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID de la solicitud:</strong> <code>@Model.RequestId</code>
    </p>
}

<p>Si el problema persiste, contacta a nuestro soporte técnico con el ID de la solicitud mencionado arriba.</p>

<a href="/Home" class="btn btn-primary mt-3">Volver al inicio</a>